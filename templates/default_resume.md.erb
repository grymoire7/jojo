###### [[<%= website.sub(/^https?:\/\//, '') %>](<%= website %>)] . [ <%= email %> ] . [ <%= phone %> ]

<%= summary %>

## Skills

<%= skills.join(" • ") %>

<% unless [linkedin, github].compact.empty? -%>
###### <%= [linkedin, github].compact.map { |url| "[[#{url.sub(/^https?:\/\//, '')}](#{url})]" }.join(' . ') %>
<% end -%>

## Experience

<% experience.each do |exp| %>
### <%= exp["title"] %> at <%= exp["company"] %>
*<%= exp["start_date"] %> - <%= exp["end_date"] %>*

<%= exp["description"] %>

**Technologies:** <%= exp["technologies"].join(", ") %>
<% end %>

<% if education && !education.empty? %>
## Education

<% education.each do |edu| %>
### <%= edu["degree"] %> - <%= edu["institution"] %>
<%= edu["description"] if edu["description"] %>
<% end %>

<% end %>

<% if recommendations && !recommendations.empty? %>
## Recommendations

<% recommendations.each do |rec| %>
> <%= rec["quote"] %>
<% if rec["name"] %>
— **<%= rec["name"] %><% if rec["title"] %>, <%= rec["title"] %><% end %>**
<% end %>

<% end %>
<% end %>

<% if projects && !projects.empty? %>
## Projects & Achievements

<% projects.each do |proj| %>
- **<%= proj["name"] %>**: <%= proj["description"] %> (Skills: <%= proj["skills"].join(", ") %>)

<% end %>
<% end %>

